<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lehrveranstaltung">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lehrveranstaltung">


<!--
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Lehrveranstaltung.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

-->
        <activity
            android:name=
                "com.example.lehrveranstaltung.LehrveranstaltungsVerwaltungHS"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER"
                    />
            </intent-filter>
        </activity>

        <activity
            android:name=".LoginAAS"
            android:label="@string/app_name"
            android:theme="@style/Theme.Lehrveranstaltung.NoActionBar">
        </activity>

        <activity
            android:name=".AdministratorAS"
            android:label="@string/app_name"
            android:theme="@style/Theme.Lehrveranstaltung.NoActionBar">
        </activity>


        <activity
            android:name=".AdminSachbearbeiterEditierenAAS"
            android:label="@string/app_name"
            android:theme="@style/Theme.Lehrveranstaltung.NoActionBar">
        </activity>

        <activity
            android:name=".AdminSachbearbeiterErstellenAAS"
            android:label="@string/app_name"
            android:theme="@style/Theme.Lehrveranstaltung.NoActionBar">
        </activity>

        <activity
            android:name=".SachbearbeiterSachbearbeiterEditierenAAS"
            android:label="@string/app_name"
            android:theme="@style/Theme.Lehrveranstaltung.NoActionBar">
        </activity>

        <activity
            android:name=".SachbearbeiterAuswaehlenAAS"
            android:label="@string/app_name"
            android:theme="@style/Theme.Lehrveranstaltung.NoActionBar">
        </activity>

        <activity
            android:name=".SachbearbeiterAS"
            android:label="@string/app_name"
            android:theme="@style/Theme.Lehrveranstaltung.NoActionBar">
        </activity>

    </application>

</manifest>